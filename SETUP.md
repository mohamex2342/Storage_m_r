# 🚀 Cloud Hunter - دليل التشغيل السريع

## بدء التشغيل

### الطريقة 1: فتح الملف مباشرة
افتح ملف `index.html` في أي متصفح ويب حديث.

### الطريقة 2: باستخدام خادم محلي (موصى به)

#### Python (إذا كان مثبتاً)
```bash
# Python 3
python -m http.server 8000

# Python 2
python -m SimpleHTTPServer 8000
```

ثم افتح المتصفح على: `http://localhost:8000`

#### Node.js (إذا كان مثبتاً)
```bash
npx http-server -p 8000
```

ثم افتح المتصفح على: `http://localhost:8000`

#### PHP (إذا كان مثبتاً)
```bash
php -S localhost:8000
```

ثم افتح المتصفح على: `http://localhost:8000`

## ⚠️ ملاحظات مهمة

### إعداد Firebase
للتأكد من عمل Firebase بشكل صحيح:
1. تأكد من أن مشروع Firebase موجود
2. تأكد من تفعيل Authentication (Email/Password)
3. تأكد من تفعيل Firestore Database
4. تأكد من قواعد الأمان في Firestore:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Users collection
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Files collection
    match /files/{fileId} {
      allow read, write, delete: if request.auth != null && 
        resource.data.userId == request.auth.uid;
      allow create: if request.auth != null;
    }
  }
}
```

### إعداد Telegram Bot
تأكد من:
1. البوت نشط وموجود
2. الـ Token صحيح
3. الـ Chat ID صحيح
4. البوت لديه صلاحيات إرسال الملفات

### إعداد Bit.ly
تأكد من:
1. الـ Access Token صحيح وفعّال
2. لديك حصة متاحة في الاستخدام

## 🧪 اختبار الموقع

### 1. إنشاء حساب
- انتقل إلى صفحة إنشاء الحساب
- أدخل اسمك، بريدك الإلكتروني، وكلمة مرور
- انقر على "إنشاء الحساب"

### 2. رفع ملف تجريبي
- انتقل إلى قسم "رفع الملفات"
- اسحب ملف صغير (مثل صورة أو مستند)
- انتظر اكتمال الرفع

### 3. التحقق من الملفات
- انتقل إلى "مكتبة الملفات"
- تأكد من ظهور الملف المرفوع
- جرب تحميل الملف
- جرب نسخ الرابط المختصر

## 🐛 حل المشاكل الشائعة

### المشكلة: لا يعمل التسجيل/الدخول
**الحل:**
- تحقق من اتصال الإنترنت
- تحقق من إعدادات Firebase في console.firebase.google.com
- تأكد من تفعيل Email/Password في Authentication

### المشكلة: فشل رفع الملفات
**الحل:**
- تحقق من حجم الملف (يجب أن يكون أقل من 2GB)
- تحقق من إعدادات Telegram Bot
- تأكد من أن البوت نشط

### المشكلة: لا يعمل اختصار الروابط
**الحل:**
- تحقق من Bit.ly Access Token
- قد تكون وصلت للحد الأقصى من الطلبات
- سيتم نسخ الرابط الأصلي كبديل

### المشكلة: CORS Errors في Console
**الحل:**
- استخدم خادم محلي (لا تفتح الملف مباشرة)
- تأكد من إعدادات Firebase

## 📱 الاختبار على الأجهزة المحمولة

### باستخدام شبكة محلية
1. شغل الخادم المحلي على جهاز الكمبيوتر
2. احصل على IP الخاص بجهازك:
   - Windows: `ipconfig`
   - Mac/Linux: `ifconfig`
3. افتح المتصفح على الهاتف وانتقل إلى:
   `http://YOUR_IP:8000`

### باستخدام أدوات التطوير
- Chrome DevTools: F12 > Toggle Device Toolbar
- Firefox: F12 > Responsive Design Mode

## 🎯 الخطوات التالية

بعد التأكد من عمل الموقع:
1. قم بتخصيص الألوان والتصميم حسب رغبتك
2. أضف ميزات إضافية من القائمة في README
3. انشر الموقع على منصة استضافة:
   - Firebase Hosting
   - Netlify
   - Vercel
   - GitHub Pages

## 📞 الدعم

إذا واجهت أي مشاكل، تواصل مع المطور:
- Telegram: [@General_MRT](https://t.me/General_MRT)

---

**ملاحظة**: هذا المشروع تعليمي وقد يحتاج لتحسينات أمنية إضافية للاستخدام في بيئة إنتاجية.